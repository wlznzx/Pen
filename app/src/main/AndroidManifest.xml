<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.maxeye.einksdk1">


    <!--
        <application>
            <service android:name=".Bluetooth.BluetoothService" />
            <service android:name=".DBdata.EinkDBService"/>
        </application>
    -->


    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name= "android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:name="com.maxeye.einksdk1.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.maxeye.einksdk1.BtActivity"></activity>

        <activity android:name="com.maxeye.einksdk1.OnLineActivity">

        </activity>

        <activity android:name="com.maxeye.einksdk1.OffLineActivity">

        </activity>
        <activity android:name="com.maxeye.einksdk1.SqlitActivity">

        </activity>

        <activity android:name="com.maxeye.einksdk1.PageShowActivity">

        </activity>

        <activity android:name="com.maxeye.einksdk1.OnlineActivity2">

        </activity>


        <service android:name="com.maxeye.einksdk.Bluetooth.BluetoothService" />
        <service android:name="com.maxeye.einksdk.DBdata.EinkDBService" />
        <service android:name="com.maxeye.einksdk.MyScript.MyScriptService" />

    </application>

</manifest>